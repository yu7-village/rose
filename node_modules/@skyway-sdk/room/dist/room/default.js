"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.RoomImpl = void 0;
const default_1 = require("../member/local/default");
const base_1 = require("./base");
/**@internal */
class RoomImpl extends base_1.RoomBase {
    constructor(channel, _plugin) {
        super('default', channel);
        this._plugin = _plugin;
        this._disableSignaling = false;
    }
    static Create(context, channel) {
        return __awaiter(this, void 0, void 0, function* () {
            let room;
            const authToken = context.authToken;
            if (authToken.isSfuCreateBotEnabled({
                id: channel.id,
                name: channel.name,
            })) {
                const plugin = yield RoomImpl._createBot(context, channel);
                room = new RoomImpl(channel, plugin);
            }
            else {
                room = new RoomImpl(channel);
            }
            return room;
        });
    }
    _getTargetPublication(publicationId, publicationType) {
        return publicationType === 'sfu'
            ? this._getOriginPublication(publicationId)
            : this._getPublication(publicationId);
    }
    _createLocalRoomMember(local, room) {
        return new default_1.LocalRoomMemberImpl(local, room);
    }
    _isAcceptablePublication(p) {
        // sfuのoriginのみ除外する
        if (p.type === 'sfu' && !p.origin) {
            return false;
        }
        return true;
    }
}
exports.RoomImpl = RoomImpl;
//# sourceMappingURL=default.js.map