import type { LocalPersonAdapter, LocalStream, PublicationImpl, SkyWayChannelImpl } from '@skyway-sdk/core';
import { type LocalP2PRoomMember, LocalP2PRoomMemberImpl } from '../member/local/p2p';
import type { RoomPublication } from '../publication';
import { RoomBase, type RoomMemberInit } from './base';
import type { Room } from './default';
export interface P2PRoom extends Room {
    /**
     * @description [japanese] RoomにMemberを参加させる
     */
    join: (memberInit?: RoomMemberInit) => Promise<LocalP2PRoomMember>;
}
/**@internal */
export declare class P2PRoomImpl extends RoomBase implements P2PRoom {
    protected _disableSignaling: boolean;
    localRoomMember?: LocalP2PRoomMemberImpl;
    constructor(channel: SkyWayChannelImpl);
    protected _getTargetPublication(publicationId: string): RoomPublication<LocalStream> | undefined;
    protected _createLocalRoomMember<T extends LocalP2PRoomMemberImpl>(local: LocalPersonAdapter, room: this): T;
    protected _isAcceptablePublication(p: PublicationImpl): boolean;
}
//# sourceMappingURL=p2p.d.ts.map