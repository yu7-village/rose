"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SFURoomImpl = void 0;
const sfu_1 = require("../member/local/sfu");
const base_1 = require("./base");
/**@internal */
class SFURoomImpl extends base_1.RoomBase {
    constructor(channel, _plugin) {
        super('sfu', channel);
        this._plugin = _plugin;
        this._disableSignaling = true;
    }
    static Create(context, channel) {
        return __awaiter(this, void 0, void 0, function* () {
            const plugin = yield SFURoomImpl._createBot(context, channel);
            const room = new SFURoomImpl(channel, plugin);
            return room;
        });
    }
    _getTargetPublication(publicationId) {
        return this._getOriginPublication(publicationId);
    }
    _createLocalRoomMember(local, room) {
        return new sfu_1.LocalSFURoomMemberImpl(local, room);
    }
    _isAcceptablePublication(p) {
        // sfuのoriginとp2pを除外する
        if (p.type !== 'sfu' || !p.origin) {
            return false;
        }
        return true;
    }
}
exports.SFURoomImpl = SFURoomImpl;
//# sourceMappingURL=sfu.js.map